server:
  port: 8080
spring:
  application:
    name: customer
    #datasource:
    #  username: postgres
    # url: jdbc:postgresql://localhost:5432/notification
    # password: postgres
  #jpa:
  #  properties:
  #    hibernate:
  #      dialect: org.hibernate.dialect.PostgreSQLDialect
  #      format_sql: true
  #  hibernate:
  #    ddl-auto: create-drop
  #  show-sql: true
  zipkin:
    base-url: http://zipkin:9411/
  rabbitmq:
    addresses: rabbitmq:5672
eureka:
  client:
    service-url:
      default-Zone: http://eureka-server:8762/eureka